<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tsa.ecommerce">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ts"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ts"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"

        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Show"
            android:theme="@style/Theme.MaterialComponents.Light"/>


        <activity android:name=".activity.MyAccount" />
        <activity android:name=".activity.SubCategoryActivity" />
        <activity android:name=".activity.Cart" />
        <activity android:name=".activity.BookOrder" />

    </application>

</manifest>